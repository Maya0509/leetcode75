package interval_sets;

import java.util.HashMap;
import java.util.Map;

public class Solution435NonOverlappingIntervals {
    class Solution {
        // dp O(n^2) 10^5的数据量会超时（至多处理10^7(对于O(n))）
        public int eraseOverlapIntervalsDp(int[][] intervals) {
            return 0;
        }

        // greedy
        public int eraseOverlapIntervals(int[][] intervals) {
            return 0;
        }
    }
}
